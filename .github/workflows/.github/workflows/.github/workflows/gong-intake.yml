name: Gong Intake

on:
  workflow_dispatch:

permissions:
  contents: write
  issues: write

jobs:
  intake:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Placeholder intake step
        run: |
          echo "Gong intake workflow is set up."
          echo "Next step: add scripts/poll_gong_calls.py"

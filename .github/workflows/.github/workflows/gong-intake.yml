name: Gong Intake

on:
  workflow_dispatch:

permissions:
  contents: write
  issues: write

jobs:
  intake:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - name: Create test approval issue and payload
        run: |
          python scripts/poll_gong_calls.py
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
